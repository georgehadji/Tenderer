# src/tenderer/apps/alerts/migrations/: CONTEXT

Generated by `manage.py makemigrations`; never edited by hand. CI runs `makemigrations --check`.

| File | Contains / does |
|---|---|
| `0001_initial.py` | Tables `deadline` and `outbox` (unique `idempotency_key`) with `tenant_id`, and the proxy model `UpcomingDeadline`. |
