# src/tenderer/apps/documents/templates/: CONTEXT

Django template folder of the documents app (`APP_DIRS`). Templates are code, never user input (§6.6).

| Folder | Contains |
|---|---|
| `documents/` | The client-plan e-mail; see its `CONTEXT.md` |
