# docs/: CONTEXT

Tender-agnostic strategy and design documents. Anything specific to one tender belongs in `tenders/<id>/`, not here.

| File | Contains / does |
|---|---|
| `plan.md` | Business plan v2 (Greek): changes from v1 (§0), verified market facts (§1), product and responsibility matrix (§2–3), go/no-go cost formula and offer validator rules (§5), roadmap with gates (§6), business-model hypotheses (§8), risks and compliance (§9), next deliverable (§10), sources. §3 (workflow), §4 (requirements) and §7 (architecture) point to their own files instead of repeating them; §1, §6 and §10 point to the step 1 desk research (`tenders/pkm-meth-student-transport-dsa-2026/desk-research-2026.md`), which proposes gate N; §10 also points to the step 2 interview kit and the step 3 and step 4 concierge kits (`discovery/`); §5 points to the step 4 spreadsheet that implements its formula. |
| `architecture.md` | Target architecture (English), safety and security first: decisions AD1–AD21 (§0), drivers and harms (§1), principles (§2), style options and why a modular monolith with ports and adapters and a functional core (§3), stack (§4), module map, dependency rules and how the design scales to every sector, procedure type and country through sector and jurisdiction packs (§5, §5.5), each module's paradigm, patterns and controls incl. offer shapes, packs, lifecycle templates, the OCDS-shaped opportunity model and matching (§6, summary §6.11), key flows (§7), data model and classification incl. `tenant_id` (§8), deployment (§9), security architecture incl. STRIDE, access, supply chain, GDPR/Greek-law positions (§10), hazard log (§11), phases v0–v5 (§12), verification (§13), open/UNVERIFIED items (§14), sources (§15). Accepted for v1; v1 (M0–M8) built on 2026-09-24 and 2026-09-25. |
| `build-plan.md` | Build plan for the whole codebase (English), v1 built on 2026-09-24 and 2026-09-25 (progress table §2.1, with what only go-live can close): the answer in one page with the growth axes and how each is absorbed (§0), phases v1–v5 with triggers, exit gates and effort guesses (§1), v1 milestones M0–M8 with deliverables, acceptance criteria and the evidence that keeps each in v1 (§2), the scalability acceptance test for sector #2 (§3), v2 sources and matching (§4), v3 extraction (§5), v4 portal and v5 tenancy and second country (§6), definition of done (§7), open items (§8). Points to `architecture.md` for design. |
| `asvs-l2-review.md` | OWASP ASVS 5.0 Level 2 self-review of v1 (English), chapter by chapter: what applies, the control, the evidence (code, test or script), and the gaps with who closes them before go-live. Written 2026-09-25 against the code, not an external audit. |
| `gdpr-v1.md` | For v1 (Greek): the record of processing (GDPR Art. 30) and the DPIA threshold assessment (Art. 35, ΑΠΔΠΧ decision 65/2018). Not legal advice; marked where a lawyer decides. |
| `llm-models.md` | LLM choice for v3 extraction through OpenRouter (English): decisions M1–M8 (§0); tasks, value-for-money method and cost (§1); evidence per candidate model (§2); chains: dual extraction plus fallback for requirements, fallback for tables, and what is not used (§3); request settings: routing, reasoning, result handling, records (§4); temperature and other sampling parameters per call, with evidence (§5); privacy and account settings (§6); evaluation gate (§7); refresh and open items (§8); sources (§9). `architecture.md` §6.9 points here; the data behind it is in `reference/`. |
